version: "3"
services:
  app:
    # Dockerfileをビルド
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      # バインドマウント
      - ./:/workspace
      - ~/.ssh/github_sid_rsa:/root/.ssh/id_rsa
    tty: true
    env_file:
      - .env
    ports:
      - "3000:3000"